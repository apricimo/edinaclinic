{
  "name": "edinaclinic",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.634.0",
    "@aws-sdk/client-secrets-manager": "^3.634.0",
    "@aws-sdk/lib-dynamodb": "^3.634.0"
  },
  "devDependencies": {
    "vitest": "^1.5.0"
  }
}
